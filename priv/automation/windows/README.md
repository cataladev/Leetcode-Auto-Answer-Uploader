# Windows Task Scheduler

Use `scripts/install_windows_task.ps1` after updating `ProjectRoot` to your local clone path.

The script registers:

- a daily 9:00 AM run
- an additional run at user logon
